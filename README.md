
  # Dashboard with CRUD Operations

  This is a code bundle for Dashboard with CRUD Operations. The original project is available at https://www.figma.com/design/VKyaHT3I3tDv5bsu9Hjv9S/Dashboard-with-CRUD-Operations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  